import Shell from "@/components/Shell";import {supabase} from "@/lib/supabase";export const dynamic="force-dynamic";
export default async function Home(){const [{count:sites},{count:keywords},{count:articles},{data:recent}]=await Promise.all([supabase.from("sites").select("*",{count:"exact",head:true}),supabase.from("keywords").select("*",{count:"exact",head:true}),supabase.from("articles").select("*",{count:"exact",head:true}),supabase.from("keywords").select("id,keyword,status").order("created_at",{ascending:false}).limit(8)]);return <Shell><header className="top"><div><div className="title">Dashboard</div><div className="subtitle">Central de produção automática de conteúdo SEO</div></div><div className="badge">● Banco conectado</div></header><section className="grid"><div className="card"><div className="label">Sites</div><div className="value">{sites??0}</div></div><div className="card"><div className="label">Palavras-chave</div><div className="value">{keywords??0}</div></div><div className="card"><div className="label">Artigos</div><div className="value">{articles??0}</div></div><div className="card"><div className="label">Status</div><div className="value">OK</div></div></section><section className="section card"><h2>Últimas palavras-chave</h2>{recent?.length?<table className="table"><thead><tr><th>Keyword</th><th>Status</th></tr></thead><tbody>{recent.map(k=><tr key={k.id}><td>{k.keyword}</td><td><span className="status">{k.status}</span></td></tr>)}</tbody></table>:<p className="muted">Nenhuma palavra-chave cadastrada ainda.</p>}</section></Shell>}